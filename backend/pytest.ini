[pytest]
testpaths = tests
pythonpath = .
markers =
    slow: performance or large-batch tests (excluded from default CI)
    live_llm: calls Groq; requires GROQ_API_KEY
filterwarnings =
    ignore::DeprecationWarning
